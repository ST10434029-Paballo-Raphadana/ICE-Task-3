@model IEnumerable<Mango_ICE_Task_2.Models.Mango>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MangoName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MangoName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MangoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MangoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MangoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
